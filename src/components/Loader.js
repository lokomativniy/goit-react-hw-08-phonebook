import { Oval } from 'react-loader-spinner';

const Loader = () => (
  <>
    <Oval
      color="#3f51b5"
      height={40}
      width={40}
      position="absolute"
      display="inline-block"
    />
  </>
);

export default Loader;
