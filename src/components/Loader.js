import { Oval } from 'react-loader-spinner';

const Loader = () => (
  <>
    <Oval
      color="#008080"
      height={40}
      width={40}
      position="absolute"
      display="inline-block"
    />
  </>
);

export default Loader;
